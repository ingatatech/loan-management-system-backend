version: '3.8'

services:
  # PostgreSQL Database (optional - for local development only)
  # db:
  #   image: postgres:15-alpine
  #   container_name: loan-mgmt-db
  #   environment:
  #     POSTGRES_USER: ${DB_USER_DEV:-loanuser}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD_DEV:-loanpass123}
  #     POSTGRES_DB: ${DB_NAME_DEV:-loandb}
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USER_DEV:-loanuser}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Application Service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loan-mgmt-app
    ports:
      - '5000:5000'
    # depends_on:
    #   db:
    #     condition: service_healthy
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=5000
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN}
      - EMAIL_USER=${EMAIL_USER}
      - EMAIL_PASS=${EMAIL_PASS}
      - FRONTEND_URL=${FRONTEND_URL}
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
    volumes:
      - ./uploads:/usr/src/app/uploads
      - ./src/config/ca.pem:/usr/src/app/dist/config/ca.pem
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# volumes:
#   postgres_data: